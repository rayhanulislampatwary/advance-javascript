let bonus = 20;

function sum(first, second){
    let result = first + second + bonus;
    return result;
}

const output = sum(12,2);
// console.log(result);  ata dile error dekhabe bc ata scope X kore pelse.
console.log(output);